---
import { Bot, Zap, GitCompare, Workflow, Briefcase, BookOpen, ChevronRight, Menu, X } from 'lucide-astro';

const navItems = [
  { name: 'AI Tools', href: '/ai-tools', icon: Bot },
  { name: 'Productivity', href: '/productivity', icon: Zap },
  { name: 'Comparisons', href: '/comparisons', icon: GitCompare },
  { name: 'Automation', href: '/automation', icon: Workflow },
  { name: 'Business', href: '/business-software', icon: Briefcase },
  { name: 'Guides', href: '/guides', icon: BookOpen },
];
---

<header class="site-header">
  <div class="container header-container">
    <a href="/" class="logo">
      <div class="logo-wrapper">
        <img src="/logo.png" alt="StackedTools Logo" class="logo-img" />
      </div>
      <span class="logo-text">Stacked<span class="highlight">Tools</span></span>
    </a>

    <nav class="desktop-nav">
      {navItems.map(item => {
        const Icon = item.icon;
        return (
          <a href={item.href} class="nav-link">
            <Icon class="nav-icon" />
            <span>{item.name}</span>
          </a>
        );
      })}
    </nav>

    <div class="header-cta">
      <a href="/guides" class="btn-tactile btn-primary-tactile nav-btn">
        <span>Explore Guides</span>
        <div class="btn-icon-wrapper">
          <ChevronRight class="btn-icon" />
        </div>
      </a>
    </div>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: var(--bg-glass);
    backdrop-filter: var(--glass-backdrop);
    border-bottom: 1px solid var(--border-color);
  }

  .header-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 4.5rem;
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: var(--text-primary);
    font-weight: 800;
    font-size: 1.25rem;
    text-decoration: none;
  }

  .logo-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    transition: transform 300ms var(--ease-out-strong);
  }

  .logo:hover .logo-wrapper {
    transform: scale(1.05) rotate(-2deg);
  }

  .logo-img {
    height: 2.5rem;
    width: auto;
    object-fit: contain;
  }

  .highlight {
    color: var(--accent-blue);
  }

  .desktop-nav {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(255, 255, 255, 0.03);
    padding: 0.35rem 0.5rem;
    border-radius: var(--radius-full);
    border: 1px solid var(--border-color);
  }

  .nav-link {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--text-secondary);
    font-size: 0.875rem;
    font-weight: 500;
    padding: 0.45rem 0.85rem;
    border-radius: var(--radius-full);
    transition: all 200ms var(--ease-out-strong);
    text-decoration: none;
  }

  .nav-icon {
    width: 1rem;
    height: 1rem;
    color: var(--text-muted);
    transition: color 200ms var(--ease-out-strong), transform 200ms var(--ease-out-strong);
  }

  .nav-link:hover {
    color: var(--text-primary);
    background: rgba(255, 255, 255, 0.06);
  }

  .nav-link:hover .nav-icon {
    color: var(--accent-blue);
    transform: translateY(-1px);
  }

  .nav-btn {
    padding: 0.55rem 1.125rem;
    font-size: 0.8125rem;
  }

  .btn-icon {
    width: 0.875rem;
    height: 0.875rem;
  }

  @media (max-width: 1024px) {
    .desktop-nav {
      display: none;
    }
  }
</style>
